Land rendered as a dot matrix with orange markers — the global-reach visual.

const TARGET_MARKETS = [{lat:51.5,lng:-0.12,label:"UK"},{lat:25.2,lng:55.27,label:"UAE"}];
<DottedWorldMap markers={TARGET_MARKETS} connections={[[0,1]]} tone="dark" />

Feed it from one config array so locations can be changed without touching the component. Markers show where VectorValue supports projects remotely — never label them as offices.
