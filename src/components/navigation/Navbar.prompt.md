Site header. Use variant="overlay" on the home hero only; every other page uses "solid".

<Navbar variant="overlay" activeHref="/" items={nav} megaGroups={expertise} cta={{label:"Book a Call",href:"/book-a-call"}} onNavigate={setRoute} />

Collapses to a hamburger drawer under 980px of container width. The CTA is the only orange element in the bar.
