Text field for the project-enquiry form. Label sits above in uppercase mono; focus turns the border orange.

<Input label="Work Email" name="email" type="email" required placeholder="name@company.com" />
<Input label="Phone Number" name="phone" type="tel" hint="Include country code" />

Mark required fields with required — the asterisk renders in orange automatically.
