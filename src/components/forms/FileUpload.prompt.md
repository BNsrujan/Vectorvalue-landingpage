Dashed drop zone for drawings and specifications on the enquiry form.

<FileUpload accept=".pdf,.dwg,.xlsx,.zip" onFiles={setFiles} />

Presentation only. Post files to a server-side endpoint that issues signed upload URLs — never write them to a public folder and never put storage keys in the client.
